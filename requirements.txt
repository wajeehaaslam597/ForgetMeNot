# ForgetMeNot — Backend Dependencies
# Install with: pip install -r requirements.txt

fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# Scheduling (reminder background job)
apscheduler==3.10.4

# TTS — Microsoft Neural Voices, no extra system install needed on Windows
edge-tts==6.1.9

# Face Recognition
deepface==0.0.99
opencv-python==4.10.0.84
numpy==1.26.4

# Database (built-in SQLite — no install needed)
# sqlite3 is part of Python standard library
